LM_STUDIO_BASE_URL=http://127.0.0.1:1234/v1
EMBEDDING_MODEL_ID=embedding-gemma-2b

# --- Code Signing (Windows) ---
# Provide path to PFX file OR data URI (e.g. data:application/x-pkcs12;base64,<base64>)
# CSC_LINK=C:\path\to\certificate.pfx
# CSC_KEY_PASSWORD=YourPfxPassword
# Optional Windows-specific overrides:
# WIN_CSC_LINK=
# WIN_CSC_KEY_PASSWORD=
# Skip signing in certain CI contexts:
# CSC_FOR_PULL_REQUEST=true

# --- Auto Update / Publishing ---
# GH_TOKEN=github_pat_xxx   # GitHub token with repo scope

# --- macOS Notarization ---
# APPLE_ID=you@example.com
# APPLE_APP_SPECIFIC_PASSWORD=xxxx-xxxx-xxxx-xxxx
# APPLE_TEAM_ID=TEAMID1234
# SKIP_NOTARIZE=1   # Set to skip notarization (CI fallback)
