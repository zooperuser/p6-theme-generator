# Build requirements for creating portable executable
# Install with: pip install -r requirements-build.txt

# Core build tools

# Python 3.13 compatible build tools
pyinstaller>=6.6.0
setuptools>=80.0.0

# Optional: for creating installers (if needed later)
# nsis>=3.0  # For Windows installer
# cx_Freeze>=6.0  # Alternative to PyInstaller

# Development and testing
pytest>=8.0.0
pytest-cov>=4.0.0

# Note: UPX can be installed separately for compression:
# Download from https://upx.github.io/ and add to PATH